IA_m – Diffraction Auditor (v5)
Manual operativo del auditor por difracción semántico-estructural
===============================================================

📌 CONTEXTO GENERAL

Este archivo documenta el funcionamiento operativo del
IA_m Diffraction Auditor (v5), un sistema de auditoría
semántico-estructural basado en difracción conceptual.

Dado un par de polos conceptuales A y B, el auditor analiza:

- Equilibrios estructurales (nodos intermedios candidatos)
- Estabilidad del equilibrio (ratio + balance)
- Drift estructural (contaminación por nodos meta o hubs)
- Refine pass (segunda pasada guiada por ejes)
- Síntesis emergente (opcional, a partir del equilibrio final)

Todo el proceso se basa en propagación estructural mediante
Personalized PageRank (PPR) sobre una versión filtrada del grafo.


────────────────────────────────────────────────────────────
📐 NÚCLEO FORMAL — SEMANTIC DIFFRACTION SCORE
────────────────────────────────────────────────────────────

Para cada nodo candidato n:

S(n) = (P_A(n) + P_B(n)) − λ · |P_A(n) − P_B(n)|

Donde:
- P_A(n): PageRank personalizado desde el polo A
- P_B(n): PageRank personalizado desde el polo B
- λ (lambda_balance): penalización de asimetría

Interpretación:
- La suma favorece nodos influidos por ambos polos
- La diferencia penaliza dominancia de uno solo
- El resultado prioriza mediadores estructurales reales,
  no hubs semánticos ni popularidad global


────────────────────────────────────────────────────────────
OPCIONES OBLIGATORIAS
────────────────────────────────────────────────────────────

--json PATH
Ruta al grafo (red_fractal.json)
👉 Imprescindible.

--a NODO
Polo A del análisis
Ejemplo:
  --a "tiempo"

--b NODO
Polo B del análisis
Ejemplo:
  --b "posición"


────────────────────────────────────────────────────────────
OPCIONES DE PROPAGACIÓN (construcción del grafo)
────────────────────────────────────────────────────────────

--modo estructura | mixto | todo

Define qué aristas entran en el cálculo.

Modo        Qué hace                                Cuándo usar
---------------------------------------------------------------------------
estructura  ❌ Ignora embedding y tipo None          Auditoría geométrica pura
mixto       ✔ Estructura + embeddings capados (≤0.2) Ver si SEM ayuda sin contaminar
todo        ✔ Usa todas las aristas                 Exploración libre / debugging

📌 Si un nodo aparece en mixto pero no en estructura,
   significa que solo existe en el plano semántico (SEM).


--alpha FLOAT
Factor de teleport de PageRank (default: 0.85)

Más alto → influencia más global
Más bajo → influencia más local

Normalmente no tocar salvo análisis fino.


--lambda_balance FLOAT
Penaliza el desequilibrio entre polos (λ).

Valor     Efecto
-----------------------------
0.3       Favorece popularidad
0.5       Balance intermedio
0.7+      Exige centro real y simétrico


────────────────────────────────────────────────────────────
FILTRO POR EJES (AXIS / ROLES)
────────────────────────────────────────────────────────────

--no_axis
❌ Desactiva completamente el filtro por ejes.

Útil para:
- detectar drift
- comprobar dependencia de roles

⚠️ Produce contaminación (tesis / antítesis / síntesis, etc.)
No usar para validar equilibrios.


--axis_only "eje:..."

Fuerza el análisis a un eje concreto.
Ejemplo:
  --axis_only "eje:tiempo:posición__tiempo__velocidad"

Ideal para:
- auditar un eje
- ver si está “relleno” o vacío
- comprobar si existe síntesis en ese eje

⚠️ Debe ser el nombre exacto del eje, no un nodo.


--exclude_auto_dualidad
Excluye ejes generados automáticamente (eje:dualidad_auto:*).

Útil cuando:
- los ejes automáticos dominan el resultado
- se quiere auditar solo estructura “humana / descubierta”


────────────────────────────────────────────────────────────
RESULTADOS Y CANTIDAD
────────────────────────────────────────────────────────────

--top N
Número de candidatos mostrados (default: 12)

--quiet
Modo silencioso (oculta prints de ejes y debug).
Útil para scripts o logs.


────────────────────────────────────────────────────────────
ESTABILIDAD DEL EQUILIBRIO
────────────────────────────────────────────────────────────

--ratio_min FLOAT
Mínimo aceptable de:
  score(top1) / score(top2)
Default: 1.35

Bajo → muchos empates
Alto → exige ganador claro


--balance_max FLOAT
Máximo desequilibrio permitido:
  |pa - pb| / (pa + pb)
Default: 0.80

Ejemplos:
0.5 → equilibrio muy centrado
0.8 → equilibrio laxo

👉 Bajarlo sirve para detectar “equilibrios” que son en realidad semi-polos.


────────────────────────────────────────────────────────────
REFINE PASS (AXIS-GUIDED REFINEMENT)
────────────────────────────────────────────────────────────

--refine
Activa segunda pasada automática si el equilibrio NO es estable.

Proceso:
1. Se identifica un equilibrio provisional eq0
2. Se extraen los ejes asociados a eq0
3. Se recalcula el equilibrio restringiendo la propagación a esos ejes

Sirve para:
- eliminar drift
- recuperar estructura latente
- resolver casos donde A y B no comparten ejes inicialmente

📌 Es una de las opciones más importantes del programa.


────────────────────────────────────────────────────────────
RESULTADOS NO AUDITABLES (UNCONSTRAINED)
────────────────────────────────────────────────────────────

Si A ∩ B = ∅ (no comparten ejes):

- El resultado se marca como UNCONSTRAINED
- La confianza se considera LOW
- No debe interpretarse como equilibrio válido

Usa --strict_axis para abortar automáticamente
cuando no existe dominio geométrico compartido.


────────────────────────────────────────────────────────────
DIAGNÓSTICO DE DRIFT (AUTOMÁTICO)
────────────────────────────────────────────────────────────

No es una flag, pero aparece en output.

Detecta nodos que:
- no comparten ejes con A ni B
- no son equilibrio / síntesis / emergente

👉 Señala contaminación estructural y hubs meta
demasiado conectados.


────────────────────────────────────────────────────────────
SÍNTESIS (OPCIONAL)
────────────────────────────────────────────────────────────

--sintesis
Activa la búsqueda de síntesis tras el equilibrio final.

Usa PPR desde:
A, B y EQ

Fórmula:
(pa + pb + pe) − λ · (|pa - pb| + |pa - pe| + |pb - pe|)


--reject_sintesis_if_polo
Rechaza la síntesis si el top1:
- es polo de una relación de dualidad
- dentro de los ejes considerados

👉 Evita falsos positivos donde un polo reaparece como síntesis.


────────────────────────────────────────────────────────────
RESUMEN DE USO
────────────────────────────────────────────────────────────

Objetivo                     Flags recomendadas
-------------------------------------------------------------
Auditoría geométrica pura    --modo estructura --refine
Ver contaminación SEM        --modo mixto
Auditar un eje concreto      --axis_only "eje:..."
Detectar drift               --no_axis
Exigir centro real           --balance_max 0.5
Buscar síntesis              --sintesis --refine
Evitar síntesis falsa        --reject_sintesis_if_polo


────────────────────────────────────────────────────────────
EJEMPLOS DE USO
────────────────────────────────────────────────────────────

[1] ¿Este eje existe realmente o es solo SEM?
python3 audit.py --json json/red_fractal.json --a "tiempo" --b "posición" --modo estructura --refine

Diagnóstico:
✔ Equilibrio estable → eje real
❌ Nada → eje solo semántico
⚠ Inestable → eje incompleto


[2] ¿Drift por nodos meta?
python3 audit.py --json json/red_fractal.json --a "tiempo" --b "posición" --no_axis --modo estructura


[3] ¿Un eje está relleno o vacío?
python3 audit.py --json json/red_fractal.json --a "tiempo" --b "posición" \
  --axis_only "eje:tiempo:posición__tiempo__velocidad" --top 30


[4] ¿Existe síntesis estructural real?
python3 audit.py --json json/red_fractal.json --a "tiempo" --b "posición" \
  --sintesis --refine --modo estructura


[5] ¿La síntesis solo existe en SEM?
# estructura
python3 audit.py --json json/red_fractal.json --a "tiempo" --b "posición" \
  --sintesis --refine --modo estructura
# mixto
python3 audit.py --json json/red_fractal.json --a "tiempo" --b "posición" \
  --sintesis --refine --modo mixto


[6] ¿Equilibrio central o sesgado?
python3 audit.py --json json/red_fractal.json --a "tiempo" --b "posición" \
  --balance_max 0.5 --refine --modo estructura


[7] ¿El refine pass salva el análisis?
python3 audit.py --json json/red_fractal.json --a "concepto_A" --b "concepto_B" \
  --refine --modo estructura


[8] Auditoría post-entrenamiento (rutina real)
# Pares críticos
python3 audit.py --json json/red_fractal.json --a "tiempo" --b "espacio" --refine --modo estructura
python3 audit.py --json json/red_fractal.json --a "frío" --b "calor" --sintesis --refine --modo estructura
# Drift global
python3 audit.py --json json/red_fractal.json --a "tiempo" --b "posición" --no_axis --modo estructura

Si esto pasa limpio → IA_m está sana.


────────────────────────────────────────────────────────────
🧠 RESUMEN CONCEPTUAL (IA_m)
────────────────────────────────────────────────────────────

estructura  = geometría del significado
mixto       = intuición semántica
refine      = atención consciente
axis_only   = foco
no_axis     = inconsciente
sintesis    = emergencia
balance/ratio = estabilidad ontológica
